name: Update Projections

on:
  schedule:
    - cron: '0 11 * * *'
    - cron: '0 12 * * *'
  workflow_dispatch:

jobs:
  project:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('backend/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-
      - name: Install dependencies
        run: |
          pip install -r backend/requirements.txt
      - name: Verify structure
        run: |
          echo "=== backend/ ==="
          ls -la backend/
          echo "=== docs/ ==="
          ls -la docs/
          echo "=== roster.json ==="
          cat roster.json
      - name: Run projections
        run: |
          cd backend
          python -u projections.py
      - name: Commit updated projections
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/data.json docs/projections_log.json
          git diff --staged --quiet || git commit -m "chore: update projections $(date -u +'%Y-%m-%d %H:%M UTC')"
          git push
